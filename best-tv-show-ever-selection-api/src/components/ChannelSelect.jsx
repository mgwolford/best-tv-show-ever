import { CHANNEL_GROUPS } from '../data/showCategories'
export default function ChannelSelect({ onSelect, onBack }) { return <main className="game-shell channel-select"><button className="text-button" type="button" onClick={onBack}>← Back to channels</button><header className="game-header"><p className="game-kicker">Channel Mode</p><h1>Pick Your Signal</h1><p>Choose the kind of television your lineup will be built from.</p></header><section className="channel-grid">{Object.entries(CHANNEL_GROUPS).map(([id, group], index) => <button key={id} className="channel-card" type="button" onClick={() => onSelect(id)}><span className="channel-card__number">0{index + 1}</span><strong>{group.name}</strong><span>{group.subtitle}</span></button>)}</section></main> }
